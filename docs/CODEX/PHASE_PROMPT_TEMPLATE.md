# Future phase implementation prompt template

Read AGENTS.md, CURRENT_STATE, approved ADRs and the named phase specification.
Confirm explicit owner authorization for that phase; prior phase completion alone does
not authorize it. Inspect owning modules and the real data/API contracts.

Before editing state outcome, files/modules, schema/migrations, API changes, domain and
company-scope risks, evidence dependencies, regression cases and verification commands.
Implement coherent reviewed batches, test, commit, update documentation and report using
AGENTS.md. Stop at the phase boundary. Never claim unexecuted or skipped checks passed.
